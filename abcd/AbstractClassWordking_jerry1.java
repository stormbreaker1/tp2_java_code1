package abcd;

// Below class is extending to an abstract class, hence it is bound to implement the methods of the 
 // abstract class

// But it can go without implementing the abstract method only if this class itself is an abstract class
 // as below.

// But if some else class is implementing this below jerry class then that class have to implement the 
 // abstract method of the mobby class and this type of thing is happening as tom class is extending 
  // to jerry class.
public abstract class AbstractClassWordking_jerry1 extends AbstractClassWorking_mobby1{

}
